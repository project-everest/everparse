typedef struct _Simple
{
  UINT32 fst;
  UINT32 snd;
} Simple;

typedef Simple Sample;

typedef struct _P
{
  Sample fst;
  Sample snd;
} P;


entrypoint
typedef struct _Pair (UINT32 x, UINT32 y)
{
  UINT32 fst { fst <= x };
  UINT32 snd { snd <= y };
} Pair;
