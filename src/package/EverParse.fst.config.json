{
    "_comment": "These settings allow to navigate through the EverParse proofs when editing and verifying F* files with Visual Studio Code and the F* VSCode Assistant extension. If you are not using the official EverParse Docker image, make sure to build EverParse before and call VSCode from a shell started with `shell.sh` from the EverParse clone.",
    "fstar_exe": "${EVERPARSE_HOME}/fstar.sh",
    "options": [
      "--load_cmxs", "evercddl_lib",
      "--load_cmxs", "evercddl_plugin"
    ],
    "include_dirs": [
      "${EVERPARSE_HOME}/src/lowparse",
      "${EVERPARSE_HOME}/src/lowparse/pulse",
      "${EVERPARSE_HOME}/src/cbor/spec",
      "${EVERPARSE_HOME}/src/cbor/pulse",
      "${EVERPARSE_HOME}/src/cddl/pulse",
      "${EVERPARSE_HOME}/src/cddl/spec",
      "${EVERPARSE_HOME}/src/cddl/tool",
      "${EVERPARSE_HOME}/lib/evercddl/plugin",
      "${EVERPARSE_HOME}/lib/evercddl/lib",
      "${EVERPARSE_HOME}/src/3d",
      "${EVERPARSE_HOME}/src/3d/prelude",
      "${EVERPARSE_HOME}/src/ASN1"
    ]
  }
  
