export
output
typedef struct _OPAIR {
  UINT32 fst;
  UINT32 snd;
} OPAIR;

export
output
typedef struct _OTRIPLE {
  UINT32 fst;
  UINT32 snd;
  UINT32 thd;
} OTRIPLE;

/* FIXME: krml refuses to generate empty .c files, so I need to detect that at the exit of Batch.process_one_c_file */
export
typedef UINT32 dummy_aa;
